language: node_js
node_js:
  - "lts/*"

cache:
  yarn: true
  directories:
    - node_modules

before_script:
  - npm install -g gatsby-cli
  - npm install -g typescript

deploy:
  provider: script
  cleanup: true
  on:
    branch: master
  edge: true
